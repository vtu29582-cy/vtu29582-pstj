class Solution {
    public boolean halvesAreAlike(String s) {
        int n = s.length();
        int count = 0;

        String vowels = "aeiouAEIOU";

        for (int i = 0; i < n / 2; i++) {
            // Count vowels in the first half
            if (vowels.indexOf(s.charAt(i)) != -1) {
                count++;
            }

            // Subtract vowels in the second half
            if (vowels.indexOf(s.charAt(i + n / 2)) != -1) {
                count--;
            }
        }

        return count == 0;
    }
}
